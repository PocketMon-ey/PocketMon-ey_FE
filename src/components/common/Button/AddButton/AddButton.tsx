import React from 'react';
import { Add, AddButtonContainer, AddContainer } from './styled';

const AddButton = () => {
  return (
    <AddButtonContainer>
      <AddContainer>
        <Add>+</Add>
      </AddContainer>
    </AddButtonContainer>
  );
};

export default AddButton;
