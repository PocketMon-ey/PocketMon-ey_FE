import styled from 'styled-components';
import { theme } from '../../../styles';

export const CardContainer = styled.div`
  width: 90%;
  height: 160px;
  border: 3px solid ${theme.colors.gray_002};
  border-radius: 15px;
  margin-bottom: 20px;
`;
